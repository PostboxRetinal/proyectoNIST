import { describe, it, expect } from 'vitest';

// A simple test to validate the test setup
describe('Basic Test Setup', () => {
  it('should pass a simple test', () => {
    expect(1 + 1).toBe(2);
  });
});
